# Article import JSON

Standalone article payloads for CMS import (not news/editorial — see [`../news/`](../news/) for that pipeline).

| File | Notes |
|------|--------|
| `ai_drones.json` | Legacy / batch article payload |
| `video_photo.json` | Legacy / batch article payload |

Publish via admin article editor or API after review.
